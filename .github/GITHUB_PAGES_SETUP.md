# GitHub Pages Deployment - Quick Start

Это краткое руководство по деплою React фронтенда на GitHub Pages.

## 🚀 Преимущества GitHub Pages

- ✅ **Бесплатно** - никаких платежей за хостинг
- ✅ **HTTPS** - SSL сертификат автоматически
- ✅ **CDN** - быстрая загрузка по всему миру
- ✅ **CI/CD** - автодеплой при push в main
- ✅ **Нет сервера** - не нужно настраивать и поддерживать

## 📋 Шаг 1: Включите GitHub Pages

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Pages**
3. В секции **Source** выберите: **GitHub Actions**
4. Сохраните изменения

## 🔐 Шаг 2: Настройте Secrets

Перейдите в **Settings** → **Secrets and variables** → **Actions**

Добавьте один обязательный secret:

| Secret Name | Значение | Описание |
|------------|----------|----------|
| `VITE_API_URL` | `https://api.yourdomain.com` | URL вашего backend API |

Опционально:
| Secret Name | Значение | Описание |
|------------|----------|----------|
| `VITE_APP_NAME` | `NFC Medical Platform` | Название приложения |

## 📝 Шаг 3: Обновите URL в коде (если нужно)

Если ваш репозиторий называется **НЕ** `username.github.io`, откройте `.github/workflows/pages-deploy.yml` и убедитесь, что строка `VITE_BASE_URL` установлена правильно:

```yaml
VITE_BASE_URL: /${{ github.event.repository.name }}/
```

Это автоматически установит правильный base path для вашего приложения.

**Примеры URL**:
- Репозиторий `myuser.github.io` → `https://myuser.github.io/`
- Репозиторий `nfc-medical` → `https://myuser.github.io/nfc-medical/`

## 🎯 Шаг 4: Деплой

Просто сделайте push в main:

```bash
git add .
git commit -m "Setup GitHub Pages deployment"
git push origin main
```

Workflow автоматически:
1. Соберет React приложение
2. Загрузит в GitHub Pages
3. Опубликует по адресу `https://<username>.github.io/<repo-name>/`

## ✅ Шаг 5: Проверьте деплой

1. Перейдите во вкладку **Actions** в репозитории
2. Дождитесь завершения workflow "Deploy to GitHub Pages"
3. Откройте **Settings** → **Pages** и скопируйте URL
4. Откройте этот URL в браузере

## 🔧 Настройка Custom Domain (Опционально)

Если хотите использовать свой домен (например, `app.yourdomain.com`):

1. В **Settings** → **Pages** → **Custom domain** введите ваш домен
2. Добавьте CNAME запись у вашего DNS провайдера:
   ```
   Type: CNAME
   Name: app
   Value: <username>.github.io
   ```
3. Дождитесь проверки DNS (может занять до 24 часов)
4. GitHub автоматически выпустит SSL сертификат

## 📊 Мониторинг деплоев

Каждый push в `main` ветку с изменениями в `web/**` запускает автоматический деплой.

Статус деплоя можно посмотреть:
- **Actions** tab → "Deploy to GitHub Pages"
- **Settings** → **Pages** → Last deployment

## 🐛 Troubleshooting

### Проблема: 404 при переходе на страницу

**Причина**: React Router требует специальной настройки для SPA

**Решение**: Файл `404.html` автоматически обрабатывается. Убедитесь, что используется `BrowserRouter` в React.

---

### Проблема: CSS/JS файлы не загружаются

**Причина**: Неправильный base path

**Решение**: Проверьте, что в `vite.config.js` установлен правильный `base`:
```javascript
base: process.env.VITE_BASE_URL || '/',
```

---

### Проблема: API запросы не работают (CORS)

**Причина**: Backend не разрешает запросы с вашего GitHub Pages домена

**Решение**: Добавьте домен GitHub Pages в CORS настройки backend:
```python
# backend/config/settings.py
CORS_ALLOWED_ORIGINS = [
    'https://<username>.github.io',
]
```

---

### Проблема: Workflow не запускается

**Причина**: GitHub Pages не включен

**Решение**:
1. Settings → Pages → Source: **GitHub Actions**
2. Сделайте новый commit

---

## 📚 Дополнительная информация

- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [Vite Static Deploy Guide](https://vitejs.dev/guide/static-deploy.html)
- [Custom Domain Setup](https://docs.github.com/en/pages/configuring-a-custom-domain-for-your-github-pages-site)

## 🆘 Нужна помощь?

Проверьте логи workflow в **Actions** tab для подробной информации об ошибках.
